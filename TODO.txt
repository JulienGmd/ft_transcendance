TODO

checks:
- ! (ts assertion)
backend:
- ✅ process.env.VAR -> config.VAR (game service done)
frontend:
- location.href = "/" -> navigate("/") (Note: Google OAuth redirect is correct as external)
- alert("msg) -> utils.showNotify("msg") (none found)
- window.addEventListener and document.addEventListener in pages scripts -> onDestroy: removeEventListener
  (Note: listeners on DOM elements are auto-cleaned when elements are removed. Only window/document need cleanup, already done in game.ts, home.ts, profile.ts)

global:
- rm console.log() ?

shared:

front:
- ✅ XSS fix in errorHandler.ts
- ✅ Fix guest/player inputs logic inversion in game.ts

user-management:

game:
- ✅ validate socket message fields from client (direction must be -1, 0, or 1)
- ✅ Fix NaN precision on division by zero

monitoring:
